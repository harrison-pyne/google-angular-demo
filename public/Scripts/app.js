var map;

(function() {
    var _app = {};
    //PRIVATE FUNCTIONS
	var initPage = function () {
		
    }

    //PUBLIC FUNCTIONS
    _app.initPage = initPage;

    //NAMESPACING FOR PUBLIC FUNCTIONS
    window.app = window.app || _app;
})();