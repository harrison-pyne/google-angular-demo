"use strict";
angular.
	module("appBody").
	component("appBody", {
		// Note: The URL is relative to our `index.html` file
		"templateUrl": "Scripts/app-body/app-body.template.html",
		"controller": function appBody($scope) {
			
		}
	}
);